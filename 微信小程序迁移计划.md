# 「慢慢来」微信小程序迁移计划

制定日期：2026-09-21。状态：规划稿，尚未实施或开通云服务。

## 1. 目标与依据

把现有网站的内容和核心功能迁移为可以在微信真机上持续使用的小程序，保留暖米白、灰绿、淡陶土色和低压力语气，重新设计移动端交互。

已检查网站任务「设计情绪支持自助网站」及本地源码：`C:/Users/17534/Documents/Codex/2026-09-19/deep-empower-slogan/outputs/manmanlai/dist`。网站任务仍在更新，开始实施时应固定迁移版本并再次核对功能清单。

当前实现是 HTML、CSS、原生 JavaScript；`content.js` 保存小事、书籍、引句和推荐映射；`logic.js` 保存排序、标准化、随机推荐等逻辑；`app.js` 混合页面渲染、事件处理和浏览器存储。个人数据保存在 `localStorage` 的 `manmanlai.v1` 中，包含 wishes、entries、todos。未见现成账户系统、云同步或导入导出界面。当前心情足迹是时间线，尚不是趋势图或心理评分。

核心范围：今天的小事、心情记录、一件件来、读一点、我的期待、行动后的感受、心情足迹、联系安心的人、记录管理。先不扩展 AI 对话、心理测评、公开社区、连续打卡或复杂统计。

## 2. 技术路线

- 小程序原生框架 + TypeScript + WXML/WXSS，使用微信开发者工具预览、真机调试和上传。
- 提取并复用纯逻辑、内容和设计变量；重新实现 DOM、HTML 字符串、hash 路由、dialog、浏览器存储和剪贴板交互。
- 第一里程碑先实现本地可用版本；第二里程碑加入用户主动开启的云同步，默认仍可只在本机使用。
- 云端建议使用 CloudBase 云函数和文档数据库。身份从可信服务端上下文获取，所有私有数据操作验证归属，不能信任客户端传入的 ownerId。
- 不因迁移而改写现有网站。小程序在独立目录开发；后续若需要网站与小程序实时同步，再设计跨端账户绑定，不能假设网站用户天然对应微信用户。
- 先使用小程序基础组件和少量自建组件，避免为少量页面引入大型 UI 框架。

建议目录：`miniprogram/pages`、`miniprogram/components`、`miniprogram/content`、`miniprogram/domain`、`miniprogram/services`、`cloudfunctions`、`tests`、`docs`。

## 3. 页面和交互迁移清单

| 页面/功能 | 保留内容 | 小程序改造与验收 |
| --- | --- | --- |
| 今天 | 时段问候、随机小事、换一个、收藏、每日一句 | 首页只突出一件小事；记心情入口始终容易找到；主要操作无需长距离滚动；不因切回微信重复刷新用户正在看的内容 |
| 记一笔心情 | 五种心情、可选文字、跳过 | 独立页面，处理键盘遮挡、返回草稿、重复点击保存；无需填写文字也可记录 |
| 开始一小步 | 编辑第一步、开始、我回来了、感受反馈 | 改成连续页面状态；退出和重新打开可以恢复未结束动作；允许放下或不记录反馈 |
| 一件件来 | 收集待办、四种优先级、先做一件、排序、完成/恢复、第一步 | 用动作菜单替代桌面下拉框；编辑和调整排序入口明确；完成可撤销；最多一件未完成待办处于“先做” |
| 读一点 | 两题问答、6 本书、推荐理由、引句和来源 | 一屏一道题，可返回修改；结果独立展示；书架为轻列表；不保存问答答案，除非未来有明确需要 |
| 我的期待 | 收藏小事、自定义想法、第一步、开始和删除 | 收拢到“我的”，避免首页堆内容；重复收藏不新增重复项 |
| 心情足迹 | 按日查看心情和行动反馈、删除记录 | 时间线首版保留；分页或渐进加载；按记录发生地的日期展示；不把分类情绪强行解释为健康分数 |
| 找人聊聊 | 可编辑开场白、复制 | 用户主动复制并自行发送；入口稳定可见；不自动访问联系人或替用户发消息 |
| 记录与隐私 | 数据说明、删除 | 新增保存状态、导入导出、云同步开关；说明当前记录究竟在本机还是云端 |

底部四个导航：今天 / 一件件来 / 读一点 / 我的。我的内部放期待、足迹、记录与隐私。输入、详情和行动流程使用独立子页面，短选择使用底部面板。

视觉要求：延续已有品牌；减少移动端首屏文字；正文优先保证可读性；点击区域建议至少 44px 等效尺寸；适配安全区、微信右上角胶囊、系统大字体和键盘；不能仅凭颜色区分状态。装饰图压缩并真机验证，不遮挡主要功能。当前主图约 1.78MB，应在接入小程序前优化。

## 4. 数据与迁移设计

- 基础实体：心情/行动记录 entries、期待 wishes、待办 todos、设置 settings、进行中的动作 activeAction。
- 每条用户记录使用稳定 ID、数据版本、创建/更新时间；记录时间保存 UTC 时间、当时当地日期及可用的时区/偏移信息，避免跨国旅行后历史记录日期漂移。
- 首次迁移保留旧 ID 与时间；旧数据没有时区时明确采用的导入解释，不猜测原始地点。
- 本地写入成功才显示“已保存在本机”；失败保留输入。云同步状态单独显示为待同步、已同步或失败可重试。
- 用户主动开启同步后才上传既有记录；关闭同步停止新上传，清楚说明云端旧副本如何处理，提供删除入口。
- 同步使用稳定 ID 和幂等操作防止重复；删除使用删除标记避免旧设备把数据恢复；并发编辑保留冲突副本或提示选择，避免静默丢失文字。
- 数据库规则与云函数归属检查分别验证。用两个测试账户证明用户不能读取、修改或删除另一人的记录；日志不记录日记正文。
- 删除账户/全部数据覆盖云记录、本地缓存和待同步队列；备份保留期限单独说明，不承诺无法实现的即时全介质删除。

旧网站数据迁移需要新增工具，不能直接从微信读取浏览器 localStorage：

1. 网站端新增“导出我的记录”，生成带 schemaVersion 的 JSON 文件。
2. 小程序提供经过真机验证的文件导入入口，检查版本、字段、大小和条数。
3. 导入前展示记录数量和冲突处理说明；确认后按 ID 合并，默认保留现有记录，禁止静默全量覆盖。
4. 同一文件重复导入不产生重复记录；错误文件不破坏现有数据。
5. 小程序也提供导出，测试文件可再次导入且字段无损；提醒导出文件含私人内容，由用户自行保管。

## 5. 分阶段执行

### P0：固定范围并检查上线条件（估计 1–2 个工作日）

- [ ] 固定当前网站版本，列出全部页面、交互和数据字段，对照上述清单更新差异。
- [ ] 建立独立小程序工程、版本管理和内容来源清单。
- [ ] 确认小程序账号主体、管理员、AppID、名称可用性和开发者权限。
- [ ] 在实际后台核对服务类目、适用备案/认证/资质、隐私配置要求；以实际自我记录和阅读功能填写，不承诺任何类目必过。
- [ ] 如要开云环境，先核对地域、目标国家访问表现、计费项和可接受月预算，避免开通不必要套餐。

交付：冻结的功能清单、工程方案、上线依赖表。验收：没有把核心功能建立在尚未确认的平台能力上。

### P1：手机交互和视觉稿（估计 2–3 个工作日）

- [ ] 完成四个主页和心情记录、行动流程、书籍详情的手机稿。
- [ ] 明确返回、取消、草稿、保存成功/失败、空数据、长文本等状态。
- [ ] 定义色彩、字号、间距、按钮和底部面板，优化主图和图标。

交付：可点击的手机流程或等效交互稿、组件规则。验收：用户无需解释即可完成一次心情记录、选择一件待办、找到推荐书；首屏信息量保持克制。

### P2：本地体验版 M1（估计 4–6 个工作日）

- [ ] 建立四个导航页和子页面，迁移内容与纯逻辑。
- [ ] 完成记录、待办、阅读、期待和行动反馈全部闭环。
- [ ] 接入本地保存和版本迁移；恢复草稿与进行中的动作。
- [ ] 完成复制开场白、删除确认、错误恢复、当地日期处理。
- [ ] 使用真实 AppID 上传体验版本，在授权体验者手机中扫码使用。

交付：功能完整、数据暂存在本机的微信体验版。验收：关闭微信重新进入记录仍在；全部核心路径可用；保存失败不会伪装成功。体验版仍不等同于任何人都可使用的正式版。

### P3：可靠数据版 M2（估计 4–6 个工作日）

- [ ] 完成网站导出、小程序导入导出及旧数据兼容。
- [ ] 完成主动开启的云同步、身份校验、归属权限、失败重试和冲突处理。
- [ ] 完成删除、退出/身份变化时缓存隔离和同步队列清理。
- [ ] 明确隐私文案、实际数据流、备份期限和云资源预算提醒。

交付：数据可迁移、可备份、可选择跨设备恢复的版本。验收：两个测试账户隔离；离线记录联网后仅同步一次；重复导入不重复；删除不复活；关闭同步不再上传。

### P4：真机测试与小范围试用（估计 3–5 个工作日，可跨一周）

- [ ] 至少覆盖 iOS、Android、小屏、大字体、键盘、弱网和离线再联网。
- [ ] 在主要目标国家实测，至少包括美国实际网络环境；验证跨午夜和跨时区记录。
- [ ] 测试页面返回、微信切后台、进程被清理、快速重复点击、云端超时、历史记录较多等情形。
- [ ] 邀请约 5–10 名自愿体验者，给出体验任务但不收集其私人情绪原文。
- [ ] 记录能否独立完成任务、界面是否催促、哪里困惑、愿不愿再次使用；修复阻断问题。

交付：测试记录、问题修复清单、发布候选版。验收：无已知数据丢失、跨用户访问或关键路径阻断；用户反馈支持当前信息结构。

### P5：正式发布 M3（开发准备估计 1–2 个工作日，外部审核另计）

- [ ] 按实际主体和类目完成后台要求的备案/认证/资质与隐私配置。
- [ ] 准备名称、图标、介绍、截图、服务类目和审核操作说明。
- [ ] 复核书籍标题、引句来源与使用范围，迁移原始出处和本站译文标记；外链入口按平台能力适配，不假定可直接打开任意网页。
- [ ] 将“只存在浏览器”的旧文案替换为与小程序真实保存方式一致的说明。
- [ ] 提交审核，处理反馈，审核通过后发布，并再次验证正式版。
- [ ] 保留上一稳定版本与兼容数据格式，写清回退步骤和故障联系入口。

交付：微信正式版入口、运维说明、版本记录。验收：非开发者身份的普通用户能够进入和使用；正式环境数据和权限验证通过。

## 6. 时间与依赖

按单人开发、范围稳定估算：约 15–24 个工作日，通常可按 3–5 周安排；账号准备、外部审核、用户反馈等待不计入工时，日历周期可能更长。M1 可以先于云同步完成，让产品尽早在手机上体验。

依赖顺序：P0 → P1 → P2 → P3 → P4 → P5。账号与发布资料可在设计开发期间准备。没有账号仍可进行代码和部分模拟器工作，但真机体验、云联调与正式发布需要实际权限。

需要项目拥有者参与的事项：管理员扫码、主体信息和相关材料、名称选择、云服务实际开通及费用预算、体验反馈。这些是后续节点的依赖，不阻止先完成设计与工程准备。

暂定决策：名称沿用「慢慢来」；中文优先；原生小程序；四个导航；本地先可用、同步主动开启；功能与网站保持相近。实施前只需在相应节点确认账号条件和云预算，不要求现在一次决定所有细节。

## 7. 后续候选，不占首版关键路径

- 用户主动选择的温和提醒：先检查实际类目可用模板和订阅授权规则，不把“每天自动推送”作为既定能力。
- 轻量日历或每周回顾：先验证分类方式与真实需求，不对情绪做诊断性推断。
- 中英双语、更多书籍与主题内容。
- 网站与小程序共用账户及实时数据；需要单独设计身份绑定。
- 如未来加入 AI，再单独设计数据发送说明、模型服务接入和质量验证。

## 8. 参考与尚需现场核验事项

- 微信官方组件/API 示例：https://github.com/wechat-miniprogram/miniprogram-demo
- CloudBase 能力介绍：https://docs.cloudbase.net/
- 数据库文档级安全规则：https://docs.cloudbase.net/database/security-rules
- 云函数调用安全规则：https://docs.cloudbase.net/cloud-function/security-rules
- 微信后台：https://mp.weixin.qq.com/

微信开放文档部分页面本次未能直接读取；因此服务类目、账号资格、备案材料、订阅消息模板、外链与发布要求全部安排在 P0/P5 用实际账号后台复核，不以本计划替代平台审核结论。

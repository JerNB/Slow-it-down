# 贡献指南

## 开始修改

按 README 启动本地 HTTP 服务。创建功能分支，保持每次提交范围清晰；修改行为时同步更新说明。优先保留温和、不催促的中文文案、键盘可用性和移动端阅读体验。

网站与小程序需明确区分。当前仅有 `web/` 网站；计划中的小程序应在独立目录开发，不能将规划能力写成已实现功能。

## 手动验收

在独立浏览器配置或测试站点中使用虚构数据，避免修改自己的真实记录：

1. 四个主导航及“我的”内部页面均可打开，刷新后导航正常。
2. 保存一条心情，刷新后可查看；删除后不再显示。
3. 收藏小事和自定义期待，重复收藏不会重复新增；完成一次行动反馈。
4. 新增多条待办，验证优先级、排序、完成和恢复；只有一条未完成待办为“先做”。
5. 完成选书问答，返回修改答案，打开书籍与文学札记来源。
6. 编辑“找人聊聊”文字并复制，确认不会自动发送。
7. 用测试数据检查清空确认与取消；模拟存储不可用时不显示虚假的保存成功。
8. 检查手机宽度、大字体、键盘导航、对话框焦点与减少动态效果设置。

运行 README 中的 JavaScript 语法检查。修改纯逻辑或存储格式时，应补充对应回归验证并说明兼容性。

## 提交问题和更改

Issue 包含复现步骤、预期与实际结果、浏览器和设备信息。PR 说明修改目的、影响范围和验证结果。截图与日志必须移除私人记录和凭据。

新增书籍或引句应提供可核查出处，保留原文、本站译文与编辑札记之间的区别。不要加入未经依据的心理诊断或治疗承诺。
